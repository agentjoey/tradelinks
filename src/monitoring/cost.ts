/**
 * Phase 1 cost projection (Operations Task 4).
 *
 * Production cost estimation stub. Currently returns 0 — a real
 * implementation would query billing APIs (e.g. Vercel usage, AI provider
 * dashboards, or aggregate PipelineRun metadata) to project monthly spend.
 * The stub is a controlled placeholder; the cost-report job's
 * getProjectedCost dep remains injectable for tests.
 */

export async function getProjectedCost(): Promise<number> {
  return 0;
}
